version: '3.2'
services:
  db:
    image: postgres:15
    environment:
      - "POSTGRES_USER=${DB_USER_TEST}"
      - "POSTGRES_PASSWORD=${DB_PASS_TEST}"
      - "POSTGRES_DB=${DB_NAME_TEST}"
    ports:
      - '5433:5432'
